import React from 'react';
import { Code, Users, Clock, Star, MoreVertical, ArrowUpRight } from 'lucide-react';

export const SessionCard = ({ session, onJoin }) => {
  // SessionCard component content
};
